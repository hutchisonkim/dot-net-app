# This file serves as a template for environment variables needed for the Docker containers.
# It includes placeholders for configuration values that can be customized.

# GitHub Runner Configuration
# GITHUB_URL should be the repository or organization URL, e.g. https://github.com/owner/repo
GITHUB_URL=
# Short-lived registration token (generate in repo/settings -> Actions -> Runners -> Add runner -> Generate token)
GITHUB_TOKEN=
RUNNER_NAME=my-runner
RUNNER_LABELS=linux,x64
RUNNER_WORKDIR=/tmp/github-runner

# Optional: Set to true to enable debug logging
DEBUG=false