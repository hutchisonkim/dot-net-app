FROM nginx:alpine
# Copy the published wwwroot into nginx html root. The build context must be the
# published client output directory containing the "wwwroot" folder contents.
COPY . /usr/share/nginx/html/
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
