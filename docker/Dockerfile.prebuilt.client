FROM nginx:alpine
# With repository root as build context, copy only the published client static
# assets (wwwroot) produced into downloaded-artifacts/client/wwwroot.
COPY downloaded-artifacts/client/wwwroot/ /usr/share/nginx/html/
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
